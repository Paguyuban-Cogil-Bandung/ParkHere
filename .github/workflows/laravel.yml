name: Deploy ParkHere

# on: [workflow_dispatch]
on:
  push:
    branches:
    - main

jobs:
  web-dev-sync:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Deploying ❤❤
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          script: |
            cd /www/wwwroot/parkhere.ourproject.my.id
            echo "${{ secrets.PASSWORD }}" | sudo -S chmod +x install.sh
            echo "${{ secrets.PASSWORD }}" | sudo -S ./install.sh

