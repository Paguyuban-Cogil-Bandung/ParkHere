name: Deploy ParkHere

# on: [workflow_dispatch]
on:
  push:
    branches:
    - main

jobs:
  web-dev-sync:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Deploying ❤❤
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: ${{ secrets.PORT }}
          script: |
            cd /www/wwwroot/parkhere.ourproject.my.id
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo git stash
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo git pull origin main
            echo "${{ secrets.PASSWORD }}" | sudo -S composer install --ignore-platform-reqs
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo php artisan optimize
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo php artisan config:cache
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo php artisan route:cache
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo php artisan view:cache
            echo "${{ secrets.PASSWORD }}" | sudo -S echo "${{ secrets.ENV_PRODUCTION }}" > .env
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo php artisan key:generate
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo cp -rf public/* .
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo mv public.php index.php
            echo "${{ secrets.PASSWORD }}" | sudo -S sudo chmod -R 777 storage bootstrap/cache

